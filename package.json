{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:docs": "npm run --workspace docs build",
    "changeset`": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "^2.24.1"
  },
  ,
  "engines": {
    "node": ">=14"
  }
}
